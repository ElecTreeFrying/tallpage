# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
stats.html
stats-*.json
.wxt
web-ext.config.ts

# Agent tooling — kept on disk, never published.
# `CLAUDE.md` carries no leading slash on purpose: it matches at every depth, so
# a new directory's doc is ignored the moment it is written rather than the next
# time someone remembers to add it.
.agents/
.claude/
.mcp.json
CLAUDE.md
tallpage-context.md

# Maintainer-only release notes and backlog — kept locally, never published.
docs/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
