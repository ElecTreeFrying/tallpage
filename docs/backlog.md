# Backlog

Future work. `TODO` is actionable now; `TABLED` is consciously deferred and carries the
condition that would reopen it.

| Status   | Item                                                     | Condition / note                                                                                                                                                                                                                                                                                  |
| -------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TODO`   | Verify the `chrome.debugger` capture in a real browser   | The engine changed after the stitching one failed on a live page; the new path is unrun. Check the attach/detach cycle leaves no banner behind, a very tall page, and a page with `position: fixed` chrome. The PDF writer itself is **proven** — it produced a valid file that opened in Preview |
| `TODO`   | Padded store-listing icon                                | The listing wants **96×96 artwork inside a 128×128 canvas**, 16px transparent margin each side. `public/icon/128.png` fills its canvas, which is right for the toolbar and wrong for the store — unpadded, it renders larger than every neighbour in the grid. A separate export into `store/`    |
| `TODO`   | Store screenshots                                        | 1280×800 preferred, 1–5, and five is the maximum so there is no reason to ship fewer. Strongest sequence: popup over a real page, the four formats, the Markdown viewer, a long capture end-to-end, the empty `host_permissions`                                                                  |
| `TABLED` | `#101828` tile behind the toolbar icon                   | Reopens if the icon proves hard to see on a light toolbar. The lime is **1.16:1** against Chrome's `#F1F3F4` and its darker sibling 1.86:1 — no palette colour clears 3:1 on both toolbars. A solid tile fixes it at 15.94:1, but it changes the artwork, so it is John's call                    |
| `TODO`   | Side panel — progress and preview                        | Spec'd as its surface but not built. Progress currently shows as toolbar badge text, and in the popup only while it happens to still be open                                                                                                                                                      |
| `TODO`   | Options page — capture defaults                          | Spec'd as its surface but not built. Default format, scale, filename pattern                                                                                                                                                                                                                      |
| `TODO`   | Stale `content.ts` references in the standards set       | §E2's surface table, §E3, §A2 and `coding-standards/CLAUDE.md` still name `entrypoints/content.ts`, which no longer exists. Those are binding files John owns — an agent does not edit them on its own initiative                                                                                 |
| `TODO`   | `decisions/0001` names `content.ts` as a plain-TS entry  | Stale in the same way, and a record is John's to correct                                                                                                                                                                                                                                          |
| `TABLED` | `/DCTDecode` instead of `/FlateDecode` for PDF           | Reopens if PDF memory bites. Lossless costs a full RGB buffer plus its compressed copy, both held at once; embedding JPEG bytes verbatim skips both, at the cost of being lossy                                                                                                                   |
| `TABLED` | Offscreen document for downloads                         | Reopens if the 64MB `data:` URL ceiling is ever hit. `URL.createObjectURL` does not exist in a service worker; an offscreen document has it, at the cost of the `offscreen` permission and a second context to keep alive                                                                         |
| `TABLED` | Firefox support                                          | **Blocked, not merely unbuilt.** Firefox has no `chrome.debugger`, so the capture engine has no equivalent there. Reopening means a second engine, not a port (§E6)                                                                                                                               |
| `TABLED` | Justification text for the `debugger` permission         | Reopens at store submission. `debugger` triggers manual review every time and reviewers ask what it is for; the answer is that full-page capture has no other correct implementation                                                                                                              |
| `TABLED` | Adopt WXT's `utils/` and `components/` auto-import roots | Reopens if explicit imports become noisy enough to matter. Declining them is what makes §P1's no-type-directories rule free here; adopting them is a §A8-shaped departure and John's call                                                                                                         |
| `TABLED` | ESLint flat config                                       | Reopens when the project has enough code for a linter to earn its keep. Three §P2 rules have stock stylistic rules and would gain teeth; two are readable-only regardless                                                                                                                         |
| `TABLED` | Prettier for templates (§A5)                             | Reopens with the first non-trivial template. §A5 already names the exact settings                                                                                                                                                                                                                 |
| `TABLED` | A refused image leaves a stray `!`                       | `![x](javascript:…)` falls through the image rule to the link rule, which drops the target and keeps the label — safe, but the `!` survives as `!x`. A `(?<!!)` lookbehind on the link rule would preserve the whole source text instead. Cosmetic; reopens if it shows up in a real capture      |
| `TABLED` | Test the Angular surfaces                                | Reopens when a surface stops moving. Reaching Angular from a test means wiring `@angular/core/testing` and a DOM environment; `storage-signal.ts` needs an injection context and is the module currently untestable for it. `tests/CLAUDE.md` records the absence                                 |
